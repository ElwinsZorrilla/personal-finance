<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../Margen.Domain/Margen.Domain.csproj" />

    <!-- El motor de clasificación es lógica pura y no sabe leer una tabla.
         Aquí vive lo que le lleva los datos y guarda lo que decide, y vive aquí
         y no en el API porque el worker clasifica lo que entra por correo y el
         API clasifica lo que entra a mano: dos llamadores, una implementación. -->
    <ProjectReference Include="../Margen.Classify/Margen.Classify.csproj" />
  </ItemGroup>

</Project>
